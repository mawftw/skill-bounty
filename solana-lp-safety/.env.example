# Optional API keys — the skill degrades gracefully to keyless sources if unset.
# Copy to .env (gitignored) and fill in.
HELIUS_API_KEY=
SOLANA_TRACKER_API_KEY=
# GOPLUS and RugCheck public endpoints work without a key.
